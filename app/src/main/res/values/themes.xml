<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PINDER" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/lighter_background</item>
        <item name="colorOnPrimary">@color/foreground_text</item>
    </style>

    <style name="Theme.PINDER" parent="Base.Theme.PINDER" />
</resources>